{#
/**
 * @file
 * Theme override for a link to image field.
 */
#}
{% for item in items %}
  <div class="image-container my-2">
    <img src="{{ item.content['#url'] }}" alt="{{ item.content['#title'] }}" class="img-fluid">
  </div>
{% endfor %}